#include <stdint.h>
#include <stdio.h>

typedef struct {
	int x;
	double y;
} the_struct;

int main() {
	the_struct s;
	int array[100];
	int ****ptr = NULL;
	the_struct *sptr;
  int a, b, res;
  a += 101;
  if (a < b)
    res = a;
  else
    res = b;
}
