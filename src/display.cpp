#include "display.hpp"

namespace interpreter {
	void Display::Alloca(const llvm::Value* address) {

	}

	ObjectPtr Display::Read(const llvm::Value* address) {

	}

	void Display::Write(const llvm::Value* address, ObjectPtr object) {

	}
}
