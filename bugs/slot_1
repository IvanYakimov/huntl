TEST CASE:
TEST_F(MatcherTest, jump) {
	auto dest = Block("dest");
	auto jump = builder_.CreateBr(dest);
	Enter(dest);
	Ret(I32(0));
}

ERROR MESSAGE:
test-matcher.out: /usr/include/llvm/IR/User.h:91: llvm::Value* llvm::User::getOperand(unsigned int) const: Assertion `i < NumOperands && "getOperand() out of range!"' failed.
Aborted

SOURCE:
Matcher::Case method
